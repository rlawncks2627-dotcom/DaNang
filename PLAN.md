# 다낭 가족여행 공유 웹앱 — 설계 계획서

## Context

내년에 **엄마·동생·나 3명**이 다낭으로 여행을 간다. 항공과 숙소는 이미 예약이 끝났고, 이제 일정을 짜고 맛집·관광지를 모으는 단계다.

지금 이 정보들은 카톡 대화방에 흩어져 있다 — 링크는 스크롤 위로 사라지고, 누가 뭘 예약했는지 다시 물어봐야 하고, 현지에서 "그 식당 이름이 뭐였지" 하며 대화방을 뒤지게 된다. 여행 후엔 경비 정산이 애매하게 남는다.

이 앱은 **세 사람이 여행 정보를 한 곳에 모으고, 같이 편집하고, 현지에서 바로 꺼내 보는 공용 공간**이다. 여행 준비 → 현지 사용 → 정산·기록까지 하나의 흐름으로 커버한다.

성공 기준은 단순하다: **엄마가 별도 설명 없이 링크를 눌러 들어와서, 혼자 무언가를 추가할 수 있으면 성공.**

---

## 확정된 결정 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 범위 | 준비 + 현지 + 정산/기록 균형 | 여행 전체 주기를 하나의 앱에서 |
| 접속 | 링크 + 이름 선택 (비밀번호 없음) | 엄마의 진입장벽 최소화 |
| 지도 | 지도 없음. 카드 리스트 + 구글맵 링크 | 지도 API 결제 등록 불필요, 가볍고 빠름 |
| 스택 | Next.js + Supabase + Vercel | 실시간 동기화·저장·배포가 전부 무료 티어 |
| 사진 | 메모/영수증 첨부용 소량 | Supabase 무료 스토리지 1GB 안에서 운용 |

---

## 사용자와 사용 시나리오

**사용자 3명** — 나(주 편집자), 엄마(주로 조회, 가끔 추가), 동생(중간)

기기는 **거의 100% 스마트폰**. 모바일 우선으로 설계하고, PC는 자연스럽게 넓어지기만 하면 된다.

핵심 시나리오:

1. *(준비)* 동생이 인스타에서 본 식당을 장소 목록에 추가 → 엄마가 하트를 누름 → 내가 일정표 Day 2에 끌어다 놓음
2. *(현지)* 택시에서 "다음 어디 가?" → 홈 화면에서 오늘 일정 확인 → 장소 카드의 구글맵 버튼 → 바로 길찾기
3. *(현지)* 엄마가 밥값을 냈다 → 정산 탭에 금액 입력 → 실시간으로 셋 다 보임
4. *(귀국 후)* 정산 탭에서 "누가 누구에게 얼마" 한 줄로 확인

---

## 화면 설계

### 🏠 홈 (대시보드)

여행 전과 여행 중에 다른 얼굴을 보여준다.

- **여행 전**: D-day 카운트다운, 준비물 진행률(예: 12/20), 최근 추가된 장소·일정, 다낭 현재 날씨
- **여행 중**: 오늘 날짜의 일정 타임라인이 최상단, 다음 일정 강조, 오늘 지출 합계
- 항상: 환율 위젯 (1,000 VND ≈ ? 원), 각 탭으로 가는 큰 버튼

### 🗓 일정표

- **Day 1 / Day 2 …** 가로 탭으로 날짜 이동
- 각 날짜는 세로 타임라인. 항목 = `시간 · 제목 · 장소(선택) · 메모`
- 항목에 장소를 연결하면 카드에 구글맵 버튼이 붙음
- 순서 변경은 **위/아래 화살표 버튼**으로 (모바일 드래그앤드롭은 오작동이 잦아 배제)
- 종류별 색 구분: 이동 / 식사 / 관광 / 휴식
- 누가 추가했는지 작게 표시

### 📍 장소

- 카테고리 탭: **맛집 · 카페 · 관광 · 쇼핑 · 마사지**
- 카드 1장의 내용: 이름(한글+현지어), 카테고리, 한 줄 설명, 대략 가격대, 참고 링크(블로그/인스타), 구글맵 열기 버튼
- **하트 투표**: 3명이 각자 하트 표시 → 카드에 `❤️❤️` 표시, 하트순 정렬 가능
- 상태 태그: `가고싶어요` / `일정에 넣음` / `다녀옴`
- 등록 방식: 이름과 링크만 넣어도 저장되게 (입력 부담 최소화)

### 💸 정산

- 지출 추가: 금액(**동/원 입력 시 자동 환산**), 항목명, 카테고리, **누가 냈나**, **누가 나눠 내나**(체크박스 3개), 영수증 사진(선택)
- 목록: 날짜별 그룹, 총합과 1인당 평균
- **정산 결과**: 최종 송금 관계를 최소 횟수로 계산해 한 줄로 — *"동생 → 나 : 142,000원"*
- 환율은 여행 시작일 시점의 값을 기본으로 잡고, 수동 조정 가능

### ✅ 준비물

- **공용 / 나 / 엄마 / 동생** 4개 섹션
- 체크는 실시간 공유 — 엄마가 체크하면 내 화면에서도 바로 채워짐
- 다낭 여행 기본 템플릿을 미리 넣어둠 (여권, 유심/eSIM, 자외선차단제, 상비약, 우산, 수영복, 슬리퍼, 보조배터리, 변환플러그, 달러 환전 등)
- 항목 추가/삭제 자유

### 🎫 예약정보

여행 중 가장 급하게 찾게 되는 정보들. **검색 없이 한 화면에서 바로 보이는 것**이 목적.

- 항공: 편명, 출발/도착 시각, 예약번호, 좌석
- 숙소: 이름, 주소(**베트남어 주소 병기 — 택시 기사에게 보여줄 용도**), 체크인/아웃 시각, 예약번호, 연락처
- 픽업/투어: 미팅 장소·시간·바우처 번호
- 각 항목에 **주소 복사 버튼**과 스크린샷 첨부 가능
- 비상 정보 고정 블록: 여권 분실 시 영사관 연락처, 현지 긴급번호

### 📝 기록

- 한 줄 메모 + 사진 1~2장을 시간순으로 쌓는 타임라인
- 누가 썼는지 표시, 여행 후 스크롤하면 그대로 여행기
- 사진은 업로드 시 자동 리사이즈(긴 변 1600px) — 용량 절약

---

## 데이터 모델 (Supabase / PostgreSQL)

모든 테이블에 `trip_id`를 두어 나중에 다른 여행도 담을 수 있게 한다.

```
trips          id, title, destination, start_date, end_date, join_code, base_rate_vnd_krw
members        id, trip_id, user_id(auth), name, emoji, color
places         id, trip_id, name, name_local, category, description,
               price_level, ref_url, gmap_url, status, created_by
place_votes    place_id, member_id                       -- PK(place_id, member_id)
itinerary      id, trip_id, day_date, start_time, title, kind,
               place_id(nullable), memo, sort_order, created_by
expenses       id, trip_id, title, category, amount_vnd, amount_krw,
               paid_by, spent_at, receipt_url, memo
expense_shares expense_id, member_id                     -- 누가 나눠 내는지
checklists     id, trip_id, scope('common'|member_id), title,
               is_done, done_by, sort_order
bookings       id, trip_id, type('flight'|'hotel'|'pickup'|'etc'), title,
               confirmation_no, starts_at, address, address_local,
               phone, memo, file_url
notes          id, trip_id, member_id, content, photo_urls[], created_at
```

**실시간 동기화**: `itinerary`, `checklists`, `expenses`, `places`, `place_votes`, `notes`에 Supabase Realtime 구독을 걸어 다른 사람의 편집이 즉시 반영되게 한다.

---

## 접속 방식 (비밀번호 없는 안전한 링크)

"링크만 누르면 끝"의 UX를 유지하면서도 DB를 공개 상태로 두지 않는 방법:

1. 카톡으로 보낸 링크 = `https://<앱주소>/join/AB12CD` (초대 코드 포함)
2. 페이지 진입 시 **Supabase 익명 로그인**(`signInAnonymously`)이 자동 실행 — 사용자는 아무것도 안 함
3. `join_trip(code)` DB 함수가 코드를 검증하고 이 익명 계정을 `members`에 연결
4. **"누구세요?" 화면에서 엄마 / 동생 / 나 중 선택** — 딱 한 번
5. 이후 재방문 시 세션이 유지되어 바로 홈으로

이렇게 하면 **RLS(행 수준 보안)**를 제대로 걸 수 있다: 모든 테이블 정책은 "이 여행의 멤버인 계정만 읽기/쓰기 가능". 링크를 모르는 사람은 데이터에 접근할 수 없다.

> ⚠️ 그래도 여권번호·카드번호 같은 민감정보는 앱에 넣지 않는 것을 원칙으로 한다. 예약번호와 주소 정도까지가 적정선.

---

## 기술 스택과 구조

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | Vercel 배포가 무설정 |
| 스타일 | Tailwind CSS | 모바일 우선, 큰 글씨·큰 터치영역 |
| DB/인증/저장소 | Supabase | 실시간·RLS·스토리지 한 번에 |
| 배포 | Vercel | 무료, 푸시하면 자동 배포 |
| 환율 | `open.er-api.com` | API 키 불필요, 무료 |
| 날씨 | Open-Meteo | API 키 불필요, 무료 |
| 홈 화면 추가 | PWA manifest | 폰 홈에 아이콘으로 설치 |

**비용: 전부 무료 티어 안에서 운용 가능.**

```
danang/
  app/
    join/[code]/page.tsx      가입·이름선택
    (main)/
      page.tsx                홈
      schedule/page.tsx       일정표
      places/page.tsx         장소
      expenses/page.tsx       정산
      checklist/page.tsx      준비물
      bookings/page.tsx       예약정보
      notes/page.tsx          기록
      layout.tsx              하단 탭 내비게이션
  components/                 카드·시트·폼 등 공용 UI
  lib/
    supabase/                 클라이언트, 타입
    hooks/                    useRealtimeTable 등
    money.ts                  환율 변환, 정산 계산
  supabase/migrations/        스키마 SQL
```

---

## 구현 순서

각 단계는 그 자체로 배포해서 써볼 수 있는 상태로 끝낸다.

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **0. 기반** | Next.js 생성, Tailwind, Supabase 프로젝트 + 스키마 마이그레이션, Vercel 첫 배포 | 배포된 주소가 열림 |
| **1. 입장** | 익명 로그인 + 초대코드 + 이름 선택, 하단 탭 내비게이션, 앱 껍데기 | 폰에서 링크로 들어와 이름 선택까지 됨 |
| **2. 예약정보 + 준비물** | 가장 단순한 CRUD로 데이터 패턴·실시간 구독 방식을 확립 | 두 기기에서 체크가 즉시 동기화됨 |
| **3. 장소** | 카드 목록, 카테고리, 하트 투표, 구글맵 링크 | 장소 추가하고 하트 누르기 동작 |
| **4. 일정표** | Day 탭, 타임라인, 장소 연결, 순서 변경 | 3일치 일정을 실제로 짜볼 수 있음 |
| **5. 정산** | 지출 입력, 환율 변환, N빵 계산, 최종 송금 관계 | 샘플 지출 5건으로 정산 결과 검증 |
| **6. 기록** | 메모 + 사진 업로드(리사이즈 포함) | 사진 첨부 후 다른 기기에서 보임 |
| **7. 마무리** | 홈 대시보드 위젯(D-day·날씨·환율), PWA, 다낭 준비물 템플릿 시딩 | 폰 홈 화면에 아이콘 설치됨 |

---

## 검증 방법

- **동시 편집**: PC 브라우저 + 시크릿 창 + 실제 폰, 3개를 동시에 띄우고 한 쪽에서 체크/추가 → 나머지 두 화면에 즉시 반영되는지 확인
- **엄마 테스트**: 아무 설명 없이 카톡으로 링크만 보내고, 장소 하나를 추가해달라고 부탁 → 막히는 지점을 관찰 (이게 가장 중요한 테스트)
- **정산 정확성**: 셋이 섞여서 낸 지출 5~6건을 넣고, 손으로 계산한 값과 일치하는지 대조
- **보안**: 초대 코드 없는 브라우저에서 앱 주소로 직접 접근 → 데이터가 안 보이는지 확인
- **현지 상황 모사**: 폰을 저속 네트워크로 두고 홈·일정표가 쓸 만한 속도로 뜨는지 확인

---

## 지금은 만들지 않는 것 (YAGNI)

당장 필요 없고, 나중에 원하면 추가할 수 있는 것들:

- 지도 위 핀 표시 (구글맵 링크로 충분)
- 여러 여행 관리 (스키마는 대비되어 있으나 UI는 이번 여행 전용)
- 알림/푸시
- 본격 사진 갤러리 (갤럭시·구글포토가 이미 잘함)
- 실시간 채팅 (카톡이 있음)
- 현지 회화 카드 (번역앱이 더 나음)

---

## 다음 단계

이 계획서는 승인 완료되었다. 구현은 위 **구현 순서**의 0단계(기반 셋업)부터 시작한다.

시작 전에 준비해두면 좋은 것:

- **여행 날짜 확정** — 홈의 D-day와 일정표 Day 탭이 이 값을 기준으로 생성된다
- **예약 정보 원본** — 항공 e-티켓, 숙소 바우처 (2단계에서 앱에 입력)
- **Supabase 계정 / Vercel 계정** — 둘 다 GitHub 계정으로 가입 가능
