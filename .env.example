NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...

# 여행 초대 코드. 공유 링크는 /join/<코드> 형태다.
#
# NEXT_PUBLIC_ 접두사를 붙이지 않는다. 붙이면 브라우저 번들에 박혀서
# 앱 주소를 아는 사람이 코드를 꺼내 빈 자리를 차지할 수 있다.
# 이 값은 빌드할 때 서버에서만 읽히며, 저장소에 실제 값을 올리지 않는다.
TRIP_CODE=<invite-code>
